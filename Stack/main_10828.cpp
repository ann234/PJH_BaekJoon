#include <iostream>
#include <vector>
#include <string>

using namespace std;

void bubble_sort(std::vector<int> &arr);

int main()
{
	int N;
	cin >> N;



	return 0;
}

void stack_op(std::vector<int> stack, string op, int val = 0)
{
	if (op.compare("push") == 0)
	{

	}
}